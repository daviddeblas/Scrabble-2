import * as fromBoard from './board.actions';

describe('[Board]Boards', () => {
  it('should return an action', () => {
    expect(fromBoard.[Board]Boards().type).toBe('[Board] [Board] Boards');
  });
});
